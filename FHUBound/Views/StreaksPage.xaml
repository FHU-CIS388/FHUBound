<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="FHUBound.Views.StreaksPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:flex="clr-namespace:Flex.Controls;assembly=Flex"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ContentPage.Content>
        <StackLayout>
            <Button
                Margin="230,10,20,0"
                BackgroundColor="DarkRed"
                Clicked="CloseStreaksPage"
                CornerRadius="25"
                Text="Close" />
            <Image
                HorizontalOptions="CenterAndExpand"
                Source="calendar.png"
                VerticalOptions="CenterAndExpand" />
            <flex:FlexButton
                Margin="20,0,20,20"
                BackgroundColor="PaleVioletRed"
                ClickedCommand="{Binding AddStreakPoints}"
                ClickedCommandParameter="1200"
                CornerRadius="15"
                FontSize="23"
                HeightRequest="170"
                Text="COLLECT!"
                WidthRequest="400" />
            <!--
                ClickedCommand="{Binding Path=BindingContext.AlertPopUp, Source={x:Reference StoreCollectionView}}"
                ClickedCommandParameter="{Binding .}"
            -->
        </StackLayout>
    </ContentPage.Content>
</ContentPage>